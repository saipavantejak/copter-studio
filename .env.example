# Copter Studio — Environment Variables
# Copy this file to .env.local and fill in your values.

# Gemini API key (optional — enables AI-powered natural language simulation commands)
# Get yours at: https://aistudio.google.com/apikey
# This key stays server-side only — never bundled into the browser.
GEMINI_API_KEY=
